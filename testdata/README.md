Put completed `.replay` files here for offline tests (gitignored).

The test host also scans:

- `%LOCALAPPDATA%\FortniteGame\Saved\Demos`
- `F:\Dev\fn-lobby-info\testdata\*.replay`

Do not add live/in-progress captures. This app never parses `IsLive` files.
